services:
  - type: web
    name: luxury-estate-web
    runtime: node
    rootDir: .
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: VITE_API_URL
        value: https://luxury-estate-backend.onrender.com/api
